# 贡献指南

感谢你考虑为 Glink 贡献代码！

## 开发流程

1. Fork 本仓库
2. 创建功能分支: `git checkout -b feature/xxx`
3. 提交改动
4. 推送并创建 Pull Request

## 代码规范

- Python 3.11+
- ruff 检查通过
- 所有 API 端点要有 /health 端点
- 新增功能要有 workflow yaml 示例

## 提交规范

使用 `feat:` / `fix:` / `doc:` / `refactor:` 前缀。
